


CREATE TABLE contact
(
    contact_id       INT NOT NULL AUTO_INCREMENT,
    contact_nom      VARCHAR(250),
    contact_email    VARCHAR(250),
    contact_date     VARCHAR(8),
    PRIMARY KEY (contact_id)
);
